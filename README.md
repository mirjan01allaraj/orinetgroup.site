# ORIENT GROUP shpk — Next.js Website

Premium construction / infrastructure website built with **Next.js (App Router)** + **TailwindCSS**.

## Run
```bash
npm install
npm run dev
```

## Languages
- Default: **SQ**
- English: add `?lang=en` to any page URL (e.g. `/projects?lang=en`)

## Content
Projects and News are stored in JSON:
- `src/content/projects.sq.json` / `projects.en.json`
- `src/content/news.sq.json` / `news.en.json`


## Image pack
Images are now included in `public/images/`.
